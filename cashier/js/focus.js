document.addEventListener('DOMContentLoaded', () => {
    // Temukan elemen input search
    const searchInput = document.getElementById('product-search');
    
    // Pastikan elemen ada, lalu fokuskan kursor ke dalam input
    if (searchInput) {
        searchInput.focus();
    }
});
